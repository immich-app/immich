// generated by mobile/tool/generate_native_schema.dart, do not edit

pub const MAIN_SCHEMA_VERSION: i32 = 31;
pub const LOGS_SCHEMA_VERSION: i32 = 1;

pub mod settings {
    pub const TABLE: &str = "settings";
    pub const SQL: &str = r#"CREATE TABLE "settings" ("key" TEXT NOT NULL, "value" TEXT NULL, "updated_at" TEXT NOT NULL DEFAULT (CURRENT_TIMESTAMP), PRIMARY KEY ("key")) WITHOUT ROWID, STRICT"#;
    pub const KEY: &str = "key";
    pub const VALUE: &str = "value";
    pub const COLUMNS: &[&str] = &[KEY, VALUE];
}

pub mod logger_messages {
    pub const TABLE: &str = "logger_messages";
    pub const SQL: &str = r#"CREATE TABLE "logger_messages" ("id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT, "message" TEXT NOT NULL, "details" TEXT NULL, "level" INTEGER NOT NULL, "created_at" TEXT NOT NULL, "logger" TEXT NULL, "stack" TEXT NULL)"#;
    pub const MESSAGE: &str = "message";
    pub const DETAILS: &str = "details";
    pub const LEVEL: &str = "level";
    pub const CREATED_AT: &str = "created_at";
    pub const LOGGER: &str = "logger";
    pub const STACK: &str = "stack";
    pub const COLUMNS: &[&str] = &[MESSAGE, DETAILS, LEVEL, CREATED_AT, LOGGER, STACK];
}

pub const LOG_LEVEL_SETTING: &str = "logLevel";

#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord)]
#[repr(i64)]
pub enum LogLevel {
    All = 0,
    Finest = 1,
    Finer = 2,
    Fine = 3,
    Config = 4,
    Info = 5,
    Warning = 6,
    Severe = 7,
    Shout = 8,
    Off = 9,
}

impl LogLevel {
    pub fn from_name(name: &str) -> Option<Self> {
        match name {
            "all" => Some(Self::All),
            "finest" => Some(Self::Finest),
            "finer" => Some(Self::Finer),
            "fine" => Some(Self::Fine),
            "config" => Some(Self::Config),
            "info" => Some(Self::Info),
            "warning" => Some(Self::Warning),
            "severe" => Some(Self::Severe),
            "shout" => Some(Self::Shout),
            "off" => Some(Self::Off),
            _ => None,
        }
    }
}

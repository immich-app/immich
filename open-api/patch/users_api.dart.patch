@@
--- ../../mobile/openapi/lib/api/users_api.dart
+++ ../../mobile/openapi/lib/api/users_api.dart
@@
   Future<Response> createProfileImageWithHttpInfo(MultipartFile file,) async {
@@
-    const contentTypes = <String>['multipart/form-data'];
+    // Let `http.MultipartRequest` generate the correct multipart boundary.
+    // The default OpenAPI generator sets `Content-Type: multipart/form-data` (no boundary),
+    // which can prevent multer from parsing the upload server-side.
+    const contentTypes = <String>[];
@@
     bool hasFields = false;
     final mp = MultipartRequest('POST', Uri.parse(apiPath));
     if (file != null) {
       hasFields = true;
-      mp.fields[r'file'] = file.field;
+      // Don't add the file name as a text field; only send the file part.
       mp.files.add(file);
     }

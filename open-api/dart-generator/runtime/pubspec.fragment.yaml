#
# AUTO-GENERATED FILE, DO NOT MODIFY!
#
name: openapi
version: 1.0.0
description: Immich OpenAPI API client (generated).
publish_to: none

environment:
  # 3.12+ for sealed classes, exhaustive switches, and the modern emitted shape.
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  # `http >=1.6.0` ships the Abortable{,Streamed,Multipart}Request types the
  # generated client uses for cancellation (abortTrigger). The platform-native
  # client git pins (mertalev/http cupertino_http #1876 / ok_http #1877) live in
  # the host app's pubspec, not here — this package only needs the abortable API.
  http: ^1.6.0
  collection: ^1.19.1
  meta: ^1.16.0
  # No path dependency back into the host app: the client is self-contained.
  # The backward-compat hook (`ApiCompat`) and three-state wrapper (`Optional`)
  # are both in-library, so the dependency flows app -> openapi only.

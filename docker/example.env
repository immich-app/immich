# You can find documentation for all the supported env variables at https://immich.app/docs/install/environment-variables

# The location where your uploaded files are stored
UPLOAD_LOCATION=./library

# The location where your database files are stored. Network shares are not supported for the database
DB_DATA_LOCATION=./postgres

# To set a timezone, uncomment the next line and change Etc/UTC to a TZ identifier from this list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List
# TZ=Etc/UTC

# The Immich version to use. You can pin this to a specific version like "v1.71.0"
IMMICH_VERSION=release

# Connection secret for postgres. You should change it to a random password
# Please use only the characters `A-Za-z0-9`, without special characters or spaces
DB_PASSWORD=postgres

# The values below this line do not need to be changed
###################################################################################
DB_USERNAME=postgres
DB_DATABASE_NAME=immich

###################################################################################
# Optional: Use S3 for Immich-managed storage (originals, thumbnails, videos)
#
# Set IMMICH_STORAGE_ENGINE to 's3' and configure your S3/MinIO details below.
# If left as 'local' (default), Immich stores media under the /data mount.
#
# Notes:
# - When using S3, the /data volume is not required by the server container.
# - You do not need to set IMMICH_MEDIA_LOCATION; the server will derive
#   s3://<bucket>/<prefix> from the settings below.
# - For MinIO, set S3_ENDPOINT (e.g., http://minio:9000) and S3_FORCE_PATH_STYLE=true.
# - For AWS, omit S3_ENDPOINT and provide IAM creds or access keys below.

# Storage engine: 'local' | 's3'
IMMICH_STORAGE_ENGINE=local

# S3 connection (optional if using local engine)
# S3_ENDPOINT=
# S3_REGION=us-east-1
# S3_BUCKET=
# S3_PREFIX=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_FORCE_PATH_STYLE=false
# S3_USE_ACCELERATE=false
# S3_SSE= # AES256 | aws:kms | S3 | KMS
# S3_SSE_KMS_KEY_ID=

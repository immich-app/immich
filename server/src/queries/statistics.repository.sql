-- NOTE: This file is auto generated by ./sql-generator

-- StatisticsRepository.getMonthlyCounts
select
  extract(
    year
    from
      "asset"."fileCreatedAt"
  )::int as "year",
  extract(
    month
    from
      "asset"."fileCreatedAt"
  )::int as "month",
  count(*) as "count"
from
  "asset"
where
  "asset"."ownerId" = $1
  and "asset"."deletedAt" is null
group by
  extract(
    year
    from
      "asset"."fileCreatedAt"
  )::int,
  extract(
    month
    from
      "asset"."fileCreatedAt"
  )::int
order by
  "year" asc,
  "month" asc

-- StatisticsRepository.getTemporalMatrix
select
  extract(
    dow
    from
      "asset"."fileCreatedAt"
  )::int as "dayOfWeek",
  extract(
    hour
    from
      "asset"."fileCreatedAt"
  )::int as "hour",
  count(*) as "count"
from
  "asset"
where
  "asset"."ownerId" = $1
  and "asset"."deletedAt" is null
group by
  extract(
    dow
    from
      "asset"."fileCreatedAt"
  )::int,
  extract(
    hour
    from
      "asset"."fileCreatedAt"
  )::int
order by
  "dayOfWeek" asc,
  "hour" asc

-- StatisticsRepository.getTopPeople
select
  "person".*,
  count(distinct "asset"."id") as "count"
from
  "person"
  inner join "asset_face" on "asset_face"."personId" = "person"."id"
  inner join "asset" on "asset"."id" = "asset_face"."assetId"
  and "asset"."visibility" = 'timeline'
  and "asset"."deletedAt" is null
where
  "person"."ownerId" = $1
  and "asset_face"."deletedAt" is null
  and "asset_face"."isVisible" is true
group by
  "person"."id"
order by
  "count" desc
limit
  $2

-- StatisticsRepository.getTopCameras
select
  "asset_exif"."make",
  "asset_exif"."model",
  count(*) as "count"
from
  "asset"
  inner join "asset_exif" on "asset_exif"."assetId" = "asset"."id"
where
  "asset"."ownerId" = $1
  and "asset"."deletedAt" is null
  and "asset_exif"."make" is not null
  and "asset_exif"."model" is not null
group by
  "asset_exif"."make",
  "asset_exif"."model"
order by
  "count" desc
limit
  $2

-- StatisticsRepository.getTopLenses
select
  "asset_exif"."lensModel",
  count(*) as "count"
from
  "asset"
  inner join "asset_exif" on "asset_exif"."assetId" = "asset"."id"
where
  "asset"."ownerId" = $1
  and "asset"."deletedAt" is null
  and "asset_exif"."lensModel" is not null
group by
  "asset_exif"."lensModel"
order by
  "count" desc
limit
  $2

-- StatisticsRepository.getTopCities
select
  "asset_exif"."city",
  count(*) as "count"
from
  "asset"
  inner join "asset_exif" on "asset_exif"."assetId" = "asset"."id"
where
  "asset"."ownerId" = $1
  and "asset"."deletedAt" is null
  and "asset_exif"."city" is not null
group by
  "asset_exif"."city"
order by
  "count" desc
limit
  $2

-- StatisticsRepository.getTopCountries
select
  "asset_exif"."country",
  count(*) as "count"
from
  "asset"
  inner join "asset_exif" on "asset_exif"."assetId" = "asset"."id"
where
  "asset"."ownerId" = $1
  and "asset"."deletedAt" is null
  and "asset_exif"."country" is not null
group by
  "asset_exif"."country"
order by
  "count" desc
limit
  $2

-- StatisticsRepository.getStorageByType
select
  "asset"."type",
  count(*) as "count",
  coalesce(sum("asset_exif"."fileSizeInByte"), 0) as "size"
from
  "asset"
  left join "asset_exif" on "asset_exif"."assetId" = "asset"."id"
where
  "asset"."ownerId" = $1
  and "asset"."deletedAt" is null
group by
  "asset"."type"

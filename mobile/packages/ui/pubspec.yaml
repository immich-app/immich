name: immich_ui
publish_to: none

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  material_color_utilities: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  snapshot_test:
    path: '../snapshot_test'

flutter:
  uses-material-design: true

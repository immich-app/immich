import 'package:flutter/material.dart';

class CreateSharedAlbumPage extends StatelessWidget {
  const CreateSharedAlbumPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

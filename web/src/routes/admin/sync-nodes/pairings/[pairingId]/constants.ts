/**
 * Enough rows to see what a run is doing without turning the page into a scroll
 * through thousands of assets. The counts above the tables are the whole truth.
 */
export const ITEM_LIMIT = 100;

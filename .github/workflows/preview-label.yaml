name: Preview label (disabled)

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Preview label automation is disabled for Gallery."
